// Search Results
import Welcome from "./Welcome";
import React from 'react'

export default function ParcelResult({parcels}) {
    return (
        <div>
            console.log(parcels);
        </div>
    )
}
